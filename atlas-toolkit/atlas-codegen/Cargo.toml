[package]
name = "atlas-codegen"
version = "0.1.0"
edition = "2024"

[dependencies]
syn = { version = "2", features = ["full"] }
serde = { workspace = true, features = ["derive", "rc"] }
tera = "1.20.1"
